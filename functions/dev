if [ -f "bin/dev" ]; then
  bin/dev
elif [ -f "bin/helltool" ]; then
  bin/helltool start
elif [ -f "yarn.lock" ]; then
  yarn start
else
  echo "Don't know how to develop in this folder. Add a `bin/dev` file, or a yarn start command."
fi
